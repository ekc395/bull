// Dashboard: ticker search + AccountPanel + PositionsTable + TradeJournal.
export default function DashboardPage() {
  return (
    <main className="container mx-auto p-6">
      <h1 className="text-2xl font-semibold">Bull</h1>
      {/* TODO: TickerSearch, AccountPanel, PositionsTable, TradeJournal */}
    </main>
  );
}
