// Color-coded card: action + confidence + headline + model_used badge.
import type { VerdictResponse } from "@/types/api";

export function VerdictBanner({ verdict: _verdict }: { verdict: VerdictResponse }) {
  return null;
}
